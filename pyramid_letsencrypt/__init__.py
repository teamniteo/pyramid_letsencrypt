

def includeme(config):
    """ ctivate ``pyramid_letsencrypt``.

    Usually called via ``config.include('pyramid_letsencrypt')`` instead of
    being invoked
    directly.
    """
    print('Running includeme!')
